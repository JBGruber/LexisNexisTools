language: r
sudo: false
cache: packages

os:
  - linux
  - osx

r:
  - devel
  - release
  - 3.3.0
  - 3.2.0

script:
  - Rscript -e "install.packages('quanteda')" # can not be found on older versions of R
  - R CMD build .
  - R CMD check *tar.gz
